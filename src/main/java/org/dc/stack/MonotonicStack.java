package org.dc.stack;

/**
 *
 * 他向远方望去，无法看到高山背后的矮山，只能看到一座座更高的山峰。
 *
 * 单调栈的特性：
 * 先进先出：1）记录数据的加在最上面；2）丢掉数据也先从最上面开始
 * 单调行： 记录数据 t[i] 之前会把 <= t[i] 的数据丢掉，不可能出现上面大下面小
 *
 * 一个原则：及时去掉无用数据，保持栈中数据有序
 *
 */
public class MonotonicStack {
}
